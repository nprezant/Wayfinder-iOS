-----------
-- Up
-----------

CREATE TABLE tag(
    id INTEGER PRIMARY KEY,
    name TEXT,
    reflection INT REFERENCES reflection
        ON UPDATE CASCADE
        ON DELETE CASCADE
);
CREATE INDEX tagindex ON tag(reflection);

-----------
-- Down
-----------

DROP TABLE tag;
